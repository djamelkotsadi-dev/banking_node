{
  "name": "banking-tests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "vitest": "^1.0.0"
  },
  "vitest": {
    "testTimeout": 30000,
    "hookTimeout": 30000
  }
}
